{% for package in context.packages %}
{% capture equalsigns %}{% for count in (1..depth) %}={% endfor %}{% endcapture %}{{equalsigns}} {{ package.name }}
{{equalsigns}}= {{ package.name }}

[cols="1a,4a"]
|===

h|Description:    | {{ package.definition }}
h|Parent Package: | {{ context.name }}
h|Stereotype:     | TODO: 'package' stereotype

|===

{% if package.classes.size > 0 %}
{{equalsigns}}= Classes

{% for klass in package.classes %}
[[P-{{ package.name }}-C-{{ klass.name }}-section]]
{{equalsigns}}== {{ klass.name }}

[cols="1a,4a"]
|===

h|Definition:      | {{ klass.definition }}
h|Subclass of:     | TODO: class subclass
h|Stereotype:      | {{ klass.stereotype }}

|===

[cols="15,20,60",options="header"]
|===
|Role name |Target class and multiplicity |Definition

| TODO: role name. Was: `crs «Property»`
| TODO: link to target class and cardinality. Was: `<<SC_CRS-section,SC_CRS>> [0..1]`
| TODO: definition. Was: "`Relates to the coordinate reference system of the transformation matrix.`"

|===

[cols="15,20,60",options="header"]
|===
|Attribute |Value type and multiplicity |Definition

| TODO: attribute name. Was: `worldToTexture «Property»`
| TODO: link to target class and cardinality. Was `<<TransformationMatrix3x4-section,TransformationMatrix3x4>>`
| TODO: definition (was: "`Specifies the 3x4 transformation matrix that defines the transformation between world coordinates and texture coordinates.")

|===

NOTE: TODO SHOW THIS IF THERE IS A STEREOTYPE. Was: "`Unless otherwise specified, all attributes and role names have the stereotype «Property».`"

{% endfor %}

{% endif %}
{% if package.packages.size > 0 %}{% assign nested_depth = depth | plus: 1 %}{% include "packages", depth: nested_depth, context: package %}{% endif %}
{% endfor %}
